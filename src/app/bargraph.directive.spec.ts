import { BargraphDirective } from './bargraph.directive';

describe('BargraphDirective', () => {
  it('should create an instance', () => {
    const directive = new BargraphDirective();
    expect(directive).toBeTruthy();
  });
});
