import { Highcharts1Directive } from './highcharts1.directive';

describe('Highcharts1Directive', () => {
  it('should create an instance', () => {
    const directive = new Highcharts1Directive();
    expect(directive).toBeTruthy();
  });
});
